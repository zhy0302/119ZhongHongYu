1.ant design的引用和使用
   ant design 提供了一套非常完整的组件化设计规范与组件化编码规范，提高了产品设计效率和质量

2.axios
   Axios 是一个基于 promise 的 HTTP 库，提供几种请求方法
   当发起一个action时，当检测到action中有promise对象，他会对action中的 type,url，params
   进行判断，通过then和catch分别对成功和失败做处理，如果成功的话，会返回其value值，如果失败
   的话，就发出错误信息。

3middleware：
   提供的是位于 action 被发起之后，到达 reducer 之前的扩展点只要有一个API接口，就可以认为
   其网络请求，网络接收到请求后，成功的话给出成功后数据，失败的话就给出失败后数据。中间件一般
   放在action和reducer之间
   特点：可插拔、可无限延展、前后可依赖

4.route：每个Route就是一个路由规则，path对应路径，component即对应要显示的容器组件，即页面。
  route根据Url决定渲染哪个页面，可以用<Link to=">或goBack来进行页面的跳转

5.SPA：单页Web应用，就是只有一张Web页面的应用，SPA不做页面刷新，只做局部更新。

6.扁平化：一般用在数据整合上面，当数组里有对象，对象里又嵌套多个时，用扁平化较好，可以在对象里面立即
  找到索引，可以降低代码复杂度